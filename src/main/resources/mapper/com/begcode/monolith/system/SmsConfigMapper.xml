<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.begcode.monolith.system.repository.SmsConfigRepository">

    <!-- begcode-please-regenerate-this-file 如果您不希望重新生成代码时被覆盖，将please修改为don't ！！！-->

    <!-- 通用查询映射结果 -->
    <resultMap id="baseResultMap" type="com.begcode.monolith.system.domain.SmsConfig">
        <id column="id" property="id"/>
        <result column="id" property="id" />
        <result column="provider" property="provider" />
        <result column="sms_code" property="smsCode" />
        <result column="template_id" property="templateId" />
        <result column="access_key" property="accessKey" />
        <result column="secret_key" property="secretKey" />
        <result column="region_id" property="regionId" />
        <result column="sign_name" property="signName" />
        <result column="remark" property="remark" />
        <result column="enabled" property="enabled" />
        <result column="del_flag" property="delFlag" />
        <result column="deleted_time" property="deletedTime" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="baseColumnList">
        id,
        provider,
        sms_code,
        template_id,
        access_key,
        secret_key,
        region_id,
        sign_name,
        remark,
        enabled,
        del_flag,
        deleted_time
    </sql>

</mapper>
